# include <stdio.h>

int main(int argc, char* argv[ ])
{
	int i = 0;
	if(argc>4)
	{
		printf("too many");
		return 1;
	}

	printf("num of strings : %d\n", argc);
	
	for(i=0;i<argc;i++)
	{
		printf("%d th string : %s\n", i, argv[i]);
	}
	return 0;
}
