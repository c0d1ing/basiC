#include <stdio.h>
 
main()
{
	int i, sum=0;
	do
	{
		scanf("%d", &i);
		if(i%3 != 0)
		{
			printf("%d는 3의 배수 아님\n", i);
			continue;	// continue 존재이유 모르겠음 
		}
		sum += i;
	} while(i);
	printf("입력한 수 중 3의 배수의 총합은 %d입니다.", sum);
}
